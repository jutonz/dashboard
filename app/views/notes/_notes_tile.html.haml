.tile
  %h3.title Recent Notes
  - tile_notes.each do |note|
    = link_to notes_path(anchor: note.id) do
      %p.note-summary= note.text_abridged
  = link_to 'View all notes', notes_path, class: 'btn btn-large btn-default'
  = link_to 'Add a new note', new_note_path, class: 'btn bgn-large btn-default'
