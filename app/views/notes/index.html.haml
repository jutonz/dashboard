%h1 Notes
- if @notes.length > 10
  = link_to 'Add a note', new_note_path, class: 'btn btn-large btn-primary'
- if @notes.any? 
  - @notes.reverse.each do |note|
    = render note
- else
  %p There are currently no notes.

= link_to 'Add a note', new_note_path, class: 'btn btn-large btn-primary'